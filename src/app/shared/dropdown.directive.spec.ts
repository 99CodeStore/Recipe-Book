import { AxDropDownDirective } from './dropdown.directive';

describe('DropdownDirective', () => {
  it('should create an instance', () => {
    const directive = new AxDropDownDirective();
    expect(directive).toBeTruthy();
  });
});
